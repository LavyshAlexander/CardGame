'use strict';

describe('CardGameApp module', function() {

  beforeEach(module('CardGameApp'));

  describe('game controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var gameCtrl = $controller('GameCtrl');
      expect(gameCtrl).toBeDefined();
    }));

  });
});